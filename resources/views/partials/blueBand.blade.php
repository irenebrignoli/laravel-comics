<section id="blue-band">
  <div class="container h-100">
    <div v-for="(element, i) in elements" class="link" :key="i">
      <img src=".." alt=".." />
      <span>..</span>
    </div>
  </div>
</section>